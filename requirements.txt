# AI-TTS Requirements
# Install with: pip install -r requirements.txt

# Qt GUI framework
PySide6>=6.6.0

# Global hotkey detection
pynput>=1.7.6

# Audio playback
pygame>=2.5.0
pyaudio>=0.2.14  # For real-time streaming audio playback

# Windows clipboard access
pywin32>=306

# OpenAI API client (for DeepSeek, Qwen, etc.)
openai>=1.0.0
httpx>=0.25.0

# Markdown rendering for Quick Dictionary
markdown>=3.5.0

websocket-client>=1.7.0  # For WebSocket connections
edge-tts>=7.0.0  # For Microsoft Edge Read Aloud online TTS

# Note: VibeVoice and its dependencies (torch, transformers, numpy)
# should already be installed in your environment.
# If not, you may need to install:
# torch>=2.0.0
# transformers>=4.35.0
# numpy>=1.24.0
